{
  "name": "fitflow-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "echo 'Building...' && mkdir -p dist/public && cp client/index.html dist/public/ && echo 'Build complete'"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
